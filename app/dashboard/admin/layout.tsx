import React from "react";

// Minimal passthrough layout for the admin dashboard segment.
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
