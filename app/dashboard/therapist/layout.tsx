import React from "react";

// Minimal passthrough layout for the therapist dashboard segment.
export default function TherapistLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
